# Vendored Lua 5.4.7

Unmodified PUC-Rio Lua 5.4.7 sources used when building MatLua with the `lua`
feature (tests and optional bundled interpreter). Hosts may link their own
PUC 5.4 build instead; the hand-rolled FFI targets the 5.4 C API.

Upstream: https://www.lua.org/ftp/lua-5.4.7.tar.gz
